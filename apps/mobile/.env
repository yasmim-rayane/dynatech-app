# DynaTech — Variáveis de Ambiente
# Duplique este arquivo como .env.local para valores reais.

# URL do backend em produção (usada no Capacitor/Android)
# Em desenvolvimento, o proxy do Vite (/api -> localhost:8080) é usado automaticamente.
VITE_API_URL=http://192.168.15.2:8080/api
# VITE_API_URL=https://powdering-discharge-washhouse.ngrok-free.dev/api
